# runchengxie.github.io

Hosting the webpage of [https://runchengxie.github.io](https://runchengxie.github.io)

## Local checks

Run `script/check` before pushing. It validates post front matter, builds the site with `JEKYLL_ENV=production`, and checks internal links in `_site`.
